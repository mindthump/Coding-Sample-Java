package com.actionsets;

/**
 * A simple class to hold the largest and smallest loaded nodes in the cluster.
 * Should really use getters and setters.
 */
public class NodeLoadInfo {
    public Node biggestNode;
    public Node smallestNode;
}
